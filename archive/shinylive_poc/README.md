# Early browser prototype

The sample-size prototype is retained here for provenance. The maintained version is apps/two_proportions/app.R and uses R/sample_size_functions.R. Exported copies and runtime caches are reproducible and omitted. Randomization experiments belong to the separate randomization project and are outside this sample-size resource.
